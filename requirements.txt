# DCMI-YOLO. The paper's numbers were produced with the pinned versions below;
# newer Ultralytics releases change parse_model and the loss module, so the
# integration patch in ultralytics_patch/ may need adjusting if you upgrade.
ultralytics==8.2.0
torch==2.0.1
torchvision==0.15.2
opencv-python>=4.8.0
numpy>=1.24.0
pyyaml>=6.0
tqdm>=4.65.0
matplotlib>=3.7.0
pandas>=2.0.0
# optional, for the complexity cross-checks in scripts/
thop>=0.1.1
fvcore>=0.1.5
