# Ultralytics YOLO 🚀, AGPL-3.0 license
# Table 6 row 6 - SCI-Gamma + Star-ADown
# the two-module core of DCMI-YOLO

nc: 17
scales:
  n: [0.33, 0.25, 1024]
  s: [0.33, 0.50, 1024]
  m: [0.67, 0.75, 768]
  l: [1.00, 1.00, 512]
  x: [1.00, 1.25, 512]

backbone:
  # [from, repeats, module, args]
  - [-1, 1, SCIGamma, [3]]            # 0   low-light frontend
  - [-1, 1, Conv, [64, 3, 2]]         # 1   P1/2  (stem stays Conv)
  - [-1, 1, StarADown, [128]]          # 2   P2/4
  - [-1, 3, C2f, [128, True]]         # 3
  - [-1, 1, StarADown, [256]]          # 4   P3/8
  - [-1, 6, C2f, [256, True]]         # 5   <- P3
  - [-1, 1, StarADown, [512]]          # 6   P4/16
  - [-1, 6, C2f, [512, True]]         # 7   <- P4
  - [-1, 1, StarADown, [1024]]          # 8   P5/32
  - [-1, 3, C2f, [1024, True]]        # 9
  - [-1, 1, SPPF, [1024, 5]]          # 10   <- P5

head:
  - [-1, 1, nn.Upsample, [None, 2, 'nearest']]   # 11
  - [[-1, 7], 1, Concat, [1]]                  # 12   cat backbone P4
  - [-1, 3, C2f, [512]]                         # 13
  - [-1, 1, nn.Upsample, [None, 2, 'nearest']]   # 14
  - [[-1, 5], 1, Concat, [1]]                  # 15   cat backbone P3
  - [-1, 3, C2f, [256]]                         # 16   P3/8-small
  - [-1, 1, StarADown, [256]]          # 17
  - [[-1, 13], 1, Concat, [1]]                 # 18   cat head P4
  - [-1, 3, C2f, [512]]                         # 19   P4/16-medium
  - [-1, 1, StarADown, [512]]          # 20
  - [[-1, 10], 1, Concat, [1]]                 # 21   cat head P5
  - [-1, 3, C2f, [1024]]                        # 22   P5/32-large
  - [[16, 19, 22], 1, Detect, [nc]]          # 23   Detect(P3, P4, P5)
